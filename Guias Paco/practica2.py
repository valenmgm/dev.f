# Imprime toda la lista
lista [:]


# Desliza hacia izquierda
lista[-1]
25
# Desliza desde la 3ra posición hacia la izquierda
lista[-3:]  
[9, 16, 25]



letras = ['a', 'b', 'c', 'd', 'e', 'f', 'g']

#reemplaza algunos valores
letras[2:5] = ['C', 'D', ‘E’]l


#Remueve ciertos elementos de la lista
letras[2:5] = []
['a', 'b', 'f', 'g']

# Limpia la lista
letras[:] = []

#Contar elementos en lista
len(letters)

#Unir listas
lista.extend(letras)

# Sentencia If

if valor > minimo:
	print(valor)

# sentencia if con else
if valor > minimo:
	print(valor)
else:
	print(mminimo)

#sentencia if con elif
if valor > minimo:
	
elif valor > 0:

else

#sentencia if con or
if valor > 0 or valor > minimo:

else

#sentencia if con and
if valor > 0 and valor > minimo:

else


#Iteraciones con for

nombre = "Mi nombre"

for i in nombre
	print(i)
for i in range
	print(i)
for i in reversed(range(1, 10, 2)):
	print(i)


fruta = ['apple', 'orange', 'apple', 'pear', 'orange', 'banana']

for elemento in fruta:
	print(elemento)

for elemento in sorted(set(fruta)):
	print(elemento)


#Iterar diccionarios
for llave, valor in knights.items():
	print(llave, valor)

#variables conjuntas
a, b = 0, 1
